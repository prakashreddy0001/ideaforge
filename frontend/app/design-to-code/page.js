import DesignToCodeContent from "./DesignToCodeContent";

export const metadata = {
  title: "Design to Code",
  robots: { index: false, follow: false },
};

export default function DesignToCodePage() {
  return <DesignToCodeContent />;
}
