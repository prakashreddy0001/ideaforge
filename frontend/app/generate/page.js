import GenerateContent from "./GenerateContent";

export const metadata = {
  title: "Generate Build Package",
  robots: { index: false, follow: false },
};

export default function GeneratePage() {
  return <GenerateContent />;
}
